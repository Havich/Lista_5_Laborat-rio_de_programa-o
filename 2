/*Faça um programa que solicite ao usuário para digitar números inteiros positivos e encerre quando for digitado 0 ou valor negativo. Calcule a média dos números digitados*/

//Inclusão de bibliotecas
#include <stdio.h>

//Função principal
int main()
{
//Declaração de variáveis
int num, cont = 0;
float total = 0, soma = 0;

//Entrada de dados
do
{
    printf("Digite um valor ou digite um valor menor que um para encerrar\n");
    scanf("%d", &num);
    if(num > 0)
        {
        soma += num;
        cont++;
        }
}
while(num >= 1);

//Cálculo
total = soma / cont;

//Saída de dados
printf("A media e = %.2f\n", total);

//Fim da função principal
return 0;
}
